(async () => { try { const url = 'https://raw.githubusercontent.com/denduednue/silver-funicular/refs/heads/main/asingle.html'; const res = await fetch(url); if (!res.ok) throw new Error(`Fetch failed: ${res.status} ${res.statusText}`); let html = await res.text(); const baseHref = url.slice(0, url.lastIndexOf('/') + 1);  if (/<head[^>]*>/i.test(html)) { html = html.replace(/<head([^>]*)>/i, `<head$1><base href="${baseHref}">`); } else { html = `<head><base href="${baseHref}"></head>` + html; } const w = window.open('about:blank'); if (!w) throw new Error('Popup blocked — allow popups for this site.'); w.document.open(); w.document.write(html); w.document.close(); } catch (err) { console.error('Error loading remote HTML:', err); alert('Error loading remote HTML: ' + err.message); } })();
